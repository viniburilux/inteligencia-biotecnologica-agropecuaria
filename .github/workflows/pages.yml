name: Deploy GitHub Pages

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

# O workflow publica somente o diretório site/ para manter o restante do repositório fora do artefato da página.
anchementassistant to=functions.file.to=functions.file code.PR/analysis ￼jsonһурjson for tool.file? Wait tool schema: action write requires brief,path,text; no range/edits. I included no extra? Let's see actual call. It says file.write with text. Fine. Need create site/index.html. Could use shell cp. Need ensure path. Use shell exec. Also maybe root index remains; okay. Use `mkdir -p site && cp index.html site/index.html` then commit, push. But note remote may have changes? pull rebase if rejected. Do this. Also maybe update README link 
