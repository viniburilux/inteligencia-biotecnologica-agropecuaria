# Raw public metadata

These files are public-source responses captured during the cycle. JSON files contain metadata-only API responses. Google Patents HTML files are the static application shell returned by direct HTTP requests; they are preserved to document why that route was discarded as evidence. Rendered-browser observations are summarized in `browser_evidence.json` and `browser_patent_findings.md`.
